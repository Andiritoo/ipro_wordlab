@using Domain
@if (AllHints is not null)
{
    <FluentStack Orientation="Orientation.Vertical" HorizontalAlignment="HorizontalAlignment.Center">
        <FluentStack Orientation="Orientation.Horizontal" HorizontalAlignment="HorizontalAlignment.Center">
            <KeyboardHint Letter="Q" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'Q')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="W" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'W')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="E" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'E')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="R" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'R')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="T" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'T')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="Z" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'Z')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="U" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'U')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="I" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'I')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="O" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'O')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="P" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'P')" OnLetterClicked="@OnLetterClicked" />
        </FluentStack>

        <FluentStack Orientation="Orientation.Horizontal" HorizontalAlignment="HorizontalAlignment.Center">
            <KeyboardHint Letter="A" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'A')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="S" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'S')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="D" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'D')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="F" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'F')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="G" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'G')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="H" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'H')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="J" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'J')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="K" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'K')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="L" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'L')" OnLetterClicked="@OnLetterClicked" />
        </FluentStack>

        <FluentStack Orientation="Orientation.Horizontal" HorizontalAlignment="HorizontalAlignment.Center">
            <KeyboardHint Letter="Enter" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == null)" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="Y" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'Y')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="X" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'X')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="C" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'C')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="V" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'V')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="B" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'B')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="N" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'N')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="M" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == 'M')" OnLetterClicked="@OnLetterClicked" />
            <KeyboardHint Letter="Backspace" LetterHint="@AllHints.FirstOrDefault(x => x.Letter == null)" OnLetterClicked="@OnLetterClicked" />
        </FluentStack>

    </FluentStack>
}

@code {
    [Parameter]
    public List<LetterHint> AllHints { get; set; }

    [Parameter]
    public EventCallback<string> OnLetterClicked { get; set; }
}